from django.apps import AppConfig


class SiniestrosConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.siniestros'
    verbose_name = 'Gestión de Siniestros'
